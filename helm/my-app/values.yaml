replicaCount: 1

image:
  repository: myregistry.azurecr.io/devops-demo
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: nginx
  host: myapp.local