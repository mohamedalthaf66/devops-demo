replicaCount: 1

image:
  repository: devacrregistry123.azurecr.io/devops-demo
  tag: new
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: nginx
  host: myapp.local